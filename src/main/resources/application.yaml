spring:
  application:
    name: dm-demo

management:
  endpoint:
    shutdown:
      access: unrestricted

  endpoints:
    web:
      exposure:
        include: "*"

  server:
    error:
      include-message: always
